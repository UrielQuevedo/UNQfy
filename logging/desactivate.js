class Desactivate {

    saveEventLocal(event) {
        console.log("El log esta desactivado, por lo tanto no guarda nada.");
    }

    saveEventLoggly(event) {
        console.log("El log esta desactivado, por lo tanto no guarda nada.");
    }

}

module.exports = Desactivate;